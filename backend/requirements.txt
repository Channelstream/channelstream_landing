# Direct runtime dependencies for channelstream_landing.
#
# These mirror the install_requires list in setup.py with version floors
# chosen to keep us on releases that support Python 3.13. Transitive
# dependencies are intentionally not pinned here — pip's resolver picks
# compatible versions at build time. If we need reproducible builds later,
# move to pip-tools (`pip-compile`) and check in a fully pinned lockfile.

itsdangerous>=2.0
markdown>=3.6
plaster_pastedeploy>=1.0
pygments>=2.17
pyramid>=2.0
pyramid_debugtoolbar>=4.10
pyramid_jinja2>=2.10
requests>=2.32
waitress>=3.0
